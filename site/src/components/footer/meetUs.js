import React from 'react';

import Wrapper from './components/wrapper';

const MeetUs = (props) => {
    return <Wrapper head={'Meet us'} bod={'Sharif University Of Tehran'}/>;
};

export default MeetUs;